import request from '@/utils/axios/components-axios'

export const menu = {
  // 侧边导航接口
  menuList (query) {
    return request({
      url: '/resource/by-user/' + query,
      method: 'get'
    })
  }
}
